var m = require('mithril');

wv.layout.nav = {
    controller: function() {

    },
    view: function(ctrl) {
        return [
            m('div', {
                class: 'header'
            })
        ]
    }
}